var
  x, y;

procedure square;
var
  squ;
begin
   squ := x * x;
   ! squ;
end;

begin
   ! "How many squares do you want?";
   ? y;
   if y < 0 then ! "Naturally!";

   x := 1;
   while x <= y do
   begin
      call square;
      x := x + 1
   end
end.
