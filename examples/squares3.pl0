(* Squares *)

var
  I, J;

function Square(x);
begin
   Square := x * x;
end;

begin
   ! 'How many squares do you want?';
   ? J;
   if J < 0 then ! 'Naturally!';

   for I := 1 to J do
     ! Square(I);
end.
